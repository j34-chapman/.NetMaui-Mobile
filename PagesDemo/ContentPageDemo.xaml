<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PagesDemo.ContentPageDemo"
             Title="ContentPageDemo"
             BackgroundColor="White">
    <VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <!-- 1a) Creating a new button with a new event handler similar to main page
        now we have to create the function in the xaml.cs for it work . Now go to one .xaml.cs-->

        <!--The problem we now have is if we add this entry box and type some text into it and click the button
        and retrun to this page again the text will be gone, but this what happens on new instance of this page is
        created everytime ... but you know this mate-->
        <Entry TextColor="White"/>
        <Button Text="Click me !" Clicked="Button_Clicked"/>
    </VerticalStackLayout>
</ContentPage>

    <!--Hello testt-->